[build]
  command   = "npm run build"
  publish   = "dist"

# SPA redirect — sends all routes to index.html so React Router handles them
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
